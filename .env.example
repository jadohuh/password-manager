# Copy this file to .env and update values before running in production
SECRET_KEY=replace-with-a-secure-random-value
# For local dev use SQLite; for production use Postgres or another managed database
DATABASE_URL=sqlite:///passwords.db
FLASK_DEBUG=0
PORT=5001
